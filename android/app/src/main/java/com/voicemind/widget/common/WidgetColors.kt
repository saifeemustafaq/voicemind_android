package com.voicemind.widget.common

import androidx.glance.unit.ColorProvider
import com.voicemind.ui.theme.BrandColors

object WidgetColors {
    val Background           = ColorProvider(BrandColors.Background)
    val Label                = ColorProvider(BrandColors.OnSurface)
    val SecondaryLabel       = ColorProvider(BrandColors.OnSurfaceVariant)
    val Accent               = ColorProvider(BrandColors.Primary)
    val AccentContainer      = ColorProvider(BrandColors.PrimaryContainer)
    val OnAccent             = ColorProvider(BrandColors.OnPrimary)
    val Destructive          = ColorProvider(BrandColors.Error)
    val DestructiveContainer = ColorProvider(BrandColors.ErrorContainer)
    val OnSurface            = ColorProvider(BrandColors.OnSurface)
    val White                = ColorProvider(BrandColors.OnPrimary)
    val Warning              = ColorProvider(BrandColors.Warning)
}
